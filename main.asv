%% 

% Dobot Control


clf
clear all
close all
clc
set(0,'DefaultFigureWindowStyle','docked');
figure('Name','dobot')


% Set Variables

view(2);
ws = [-1.8 1.8 -1.8 1.8 0 2];

dobot = Dobot(ws, '1');
dobot.model.plot(zeros(1, 4))
dobot.model.teach

% dobot.calc_volume(10);
% axis equal

%%
[ faceData, vertexData, plyData{linkIndex+1} ] = plyread(['Model/d',num2str(linkIndex),'.ply'],'tri');

